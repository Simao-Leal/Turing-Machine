function A(t, s, c){
    return Math.random() > 0.5;
}

function B(t, s, c){
    return Math.random() > 0.5;
}

function C(t, s, c){
    return Math.random() > 0.5;
}

function D(t, s, c){
    return Math.random() > 0.5;
}

function E(t, s, c){
    return Math.random() > 0.5;
}

function F(t, s, c){
    return Math.random() > 0.5;
}

export let verifiers = {"A":A, "B":B, "C":C, "D":D, "E":E, "F":F};